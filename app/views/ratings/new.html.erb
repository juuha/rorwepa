<h2>Create a new rating</h2>

<%= form_for(@rating) do |f|%>
  <% if @rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rating.errors.count, "error") %> prohibited rating from being saved:</h2>

      <ul>
      <% @rating.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
  <% end %>
  
  <div class="field row">
    <%= f.label :beer, class:"col-sm-2 col-form-label"%>
    <div class="col-sm-10">
      <%= f.select(:beer_id, options_from_collection_for_select(@beers, :id, :to_s), {}, { class:"form-control form-control-lg" })%>
    </div>
  </div>

  <div class="field row">
    <%= f.label :score, class:"col-sm-2 col-form-label" %>
    <div class="col-sm-10">
      <%= f.number_field :score, class:"form-control" %>
    </div>  
  </div>

  <div class="actions">
    <%= f.submit class:"btn btn-primary"%>
  </div>

  
  
  
<% end %>
