<%= form_with(model: beer_club, local: true) do |form| %>
  <% if beer_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
      <% beer_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :beer_club_name %>
  </div>

  <div class="field">
    <%= form.label :founded %>
    <%= form.number_field :founded, id: :beer_club_founded %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city, id: :beer_club_city %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
